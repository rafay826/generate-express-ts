// jest.config.js
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  testMatch: ['**/*.spec.ts'], // Adjust if your test files have a different pattern
  // Optional: If you have specific Jest settings, you can add them here
};

